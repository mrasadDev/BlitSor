
const index = ({children}) => {
  return (
    <div style={{backgroundColor:"#fafafa", minHeight: '100vh', height: '100%', padding:"30px 40px"}}>
      {children}
    </div>
  )
}

export default index
